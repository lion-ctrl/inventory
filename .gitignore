# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Convex snapshot backups (pnpm backup / nightly task) — never in git
backups/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Local AI runtime state
.atl/
# CodeGraph index — per-checkout, rebuilt on demand, never shared
.codegraph/
